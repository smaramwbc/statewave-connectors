# Build project memory

**Build Project Memory** scans the workspace and shows a preview in the output channel. Nothing leaves your machine until you press **Ingest to Statewave**.

Ingestion is parallel and resumable; compile runs in the background — the status bar always shows whether memory is ready.
