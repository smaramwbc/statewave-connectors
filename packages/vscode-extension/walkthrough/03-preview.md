# What gets indexed

Run **Show Indexed Files** to see, before anything is sent, exactly which files are in scope and which are skipped — each with a reason.

Secret files (`.env`, `*.pem`, `id_rsa`, credentials…) are always skipped and cannot be opted in.
