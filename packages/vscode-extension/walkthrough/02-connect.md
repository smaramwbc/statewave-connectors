# Connect your server

Set `statewave.url` to your Statewave instance. It defaults to `http://localhost:8100`, so a local instance needs zero setup.

From that one setting the plugin wires the MCP server into every assistant you actually use — no second config file, no container.

Put `statewave.apiKey` in your **User** settings (never commit it).
