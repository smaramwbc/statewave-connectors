node_modules
dist
build
coverage
.next
.turbo
.cache

*.log
.DS_Store
.env
.env.*
!.env.example

.pnpm-store
pnpm-lock.yaml.bak

# pnpm pack:all output — local-only build artifacts, never committed.
tarballs

# VS Code extension packaging artifacts — never committed.
*.vsix

# Ephemeral PR body (ready for `gh pr create --body-file`, not tracked).
packages/vscode-extension/PR_BODY.md

.vscode
.idea
*.swp
AGENTS_INTERNAL.md
