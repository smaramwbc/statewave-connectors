{
  "//": "MANUAL FALLBACK ONLY. With the IDE Companion installed and statewave.mcp.autoWire on (default), this file is written/managed for you in ~/.cursor/mcp.json — you do not create it by hand. Shown here for reference and for non-Cursor MCP clients. NOTE: @statewavedev/mcp-server is not published; for local dev use the built server path instead of npx (see commented alternative).",
  "mcpServers": {
    "statewave": {
      "command": "node",
      "args": ["/absolute/path/to/statewave-connectors/packages/mcp-server/dist/cli.js"],
      "env": {
        "STATEWAVE_URL": "http://localhost:8100",
        "STATEWAVE_API_KEY": "replace-me"
      }
    }
  }
}
