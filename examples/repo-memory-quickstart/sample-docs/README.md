# Sample repo

A tiny self-contained docs tree for the [repo-memory-quickstart](../README.md) example.

The demo dry-runs the markdown connector against this folder so you can see exactly which files become which kinds of episodes — without setting up a real repo first.
